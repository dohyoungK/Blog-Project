spring:
  config:
    activate:
      on-profile: dev

  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:test
    #  datasource:
    #    url: jdbc:mysql://127.0.0.1:3306/growstory?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    #    username: smiledk
    #    password: 1112
    #    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: h2
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true